

def binary_approximation (alpha = 0,beta = 0.5, gamma = -0.1, zeta = 1.1):
	s = nn.Sigmoid((np.log(u) - np.log(1 - u) + log(alpha))/beta)
	mean_s = s(zeta - gamma) + gamma
	z = min(1,(max(0,mean_s)))
	return z